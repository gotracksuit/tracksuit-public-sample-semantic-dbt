cubes:
  - name: vw_respondent_flat_agg
    sql_table: public.vw_respondent_flat_agg
    data_source: default

    joins:

    dimensions:
      - name: agg_key
        sql: agg_key
        type: string
        primary_key: true

      - name: ethnicities
        sql: ethnicities
        type: string

      - name: wave_date
        sql: wave_date
        type: time

    measures:
      - name: weight
        sql: weight
        type: sum

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started
