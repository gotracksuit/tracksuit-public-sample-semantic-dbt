cubes:
  - name: vw_respondent_flat_ethnicity
    sql_table: public.vw_respondent_flat_ethnicity
    data_source: default

    joins: []

    dimensions:
      - name: id
        sql: id
        type: string
        primary_key: true

      - name: ethnicity_name
        sql: ethnicity_name
        type: string

      - name: ethnicities
        sql: ethnicities
        type: string

      - name: wave_date
        sql: wave_date
        type: time

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

